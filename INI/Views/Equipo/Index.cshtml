@model IEnumerable<INI.Models.DataBase.tblEquipo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equ_descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.equ_Observacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equ_descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.equ_Observacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tblEquipo_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.tblEquipo_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tblEquipo_ID })
        </td>
    </tr>
}

</table>
