@model IEnumerable<SEG.Models.DataBase.evidencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Evidencia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion_Evidencia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.actividade.Id_Contratista)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Evidencia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion_Evidencia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.actividade.Id_Contratista)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Evidencia }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Evidencia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Evidencia })
        </td>
    </tr>
}

</table>
